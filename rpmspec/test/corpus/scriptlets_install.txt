===============================================================================
Install Section (Empty)
===============================================================================

%install

-------------------------------------------------------------------------------

(spec
  (install_scriptlet
    (section_install)))

===============================================================================
Install Section (append option)
===============================================================================

%install -a
echo "appending to install"

-------------------------------------------------------------------------------

(spec
  (install_scriptlet
    (section_install)
    (scriptlet_augment_option)
    (script_block
      (script_line
        (script_content)))))

===============================================================================
Install Section (prepend option)
===============================================================================

%install -p
echo "prepending to install"

-------------------------------------------------------------------------------

(spec
  (install_scriptlet
    (section_install)
    (scriptlet_augment_option)
    (script_block
      (script_line
        (script_content)))))

===============================================================================
Install Section (literal percent in regex pattern)
===============================================================================

%install
sed -i 's/[^%]//g' file.txt

-------------------------------------------------------------------------------

(spec
  (install_scriptlet
    (section_install)
    (script_block
      (script_line
        (script_content)
        (script_content)
        (script_content)))))
