===============================================================================
Files (Empty)
===============================================================================

%files

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)))

===============================================================================
Files (Simple Path)
===============================================================================

%files
/usr/bin/tree-sitter

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (path))))

===============================================================================
Files (Expansion)
===============================================================================

%files
%{_mandir}/*/*

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (path
        (macro_expansion
          (identifier))))))

===============================================================================
Files (-n)
===============================================================================

%files -n subpackage
/usr/bin/tree-sitter

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (word)
    (file
      (path))))

===============================================================================
Files (-f)
===============================================================================

%files -f files.list

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (word)))

===============================================================================
Files (%defattr)
===============================================================================

%files
%defattr(-,root,root)
/usr/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (defattr)
    (file
      (path))))

===============================================================================
Files (%defattr with dirmode)
===============================================================================

%files
%defattr(0644,root,root,0755)
/usr/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (defattr)
    (file
      (path))))

===============================================================================
Files (%defattr with dash dirmode)
===============================================================================

%files
%defattr(-,root,root,-)
/usr/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (defattr)
    (file
      (path))))

===============================================================================
Files (%attr)
===============================================================================

%files
%attr(-, root, root) /usr/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (attr)
      (path))))

===============================================================================
Files (%attr with all dashes)
===============================================================================

%files
%attr(-,-,-) /usr/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (attr)
      (path))))

===============================================================================
Files (%attr + %dir)
===============================================================================

%files
%attr(0770,root, group) %dir /path/

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (attr)
      (file_qualifier)
      (path))))

===============================================================================
Files (%config)
===============================================================================

%files
%config /etc/tree-sitter.conf

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier
        (config_qualifier))
      (path))))

===============================================================================
Files (%doc)
===============================================================================

%files
%doc README NEWS

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier)
      (path)
      (path))))

===============================================================================
Files (%doc with subdirectory paths)
===============================================================================

%files
%doc README.md doc/CHANGELOG.md

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier)
      (path)
      (path))))

===============================================================================
Files (quoted path with spaces)
===============================================================================

%files
"/opt/bob's htdocs"

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (path))))

===============================================================================
Files (%dir)
===============================================================================

%files
%dir /usr/bin/

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier)
      (path))))

===============================================================================
Files (%dir with variables)
===============================================================================

%files
%dir %{_bindir}

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier)
      (path
        (macro_expansion
          (identifier))))))

===============================================================================
Files (%license)
===============================================================================

%files
%license LICENSE
%license NOTICES.txt

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier)
      (path))
    (file
      (file_qualifier)
      (path))))

===============================================================================
Files (%license with multiple files)
===============================================================================

%files
%license LICENSE NOTICES.txt

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier)
      (path)
      (path))))

===============================================================================
Files (%verify)
===============================================================================

%files
%verify(not size filedigest mtime) %{prefix}/bin/file

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier
        (verify))
      (path
        (macro_expansion
          (identifier))))))

===============================================================================
Files (%caps)
===============================================================================

%files
%caps(cap_net_raw=p) %{_bindir}/foo

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier
        (caps_qualifier))
      (path
        (macro_expansion
          (identifier))))))

===============================================================================
Files (%ifarch)
===============================================================================

%files
%ifarch x86_64
%{_bindir}/file
%endif

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (ifarch_statement
      (arch
        (identifier))
      (file
        (path
          (macro_expansion
            (identifier)))))))

===============================================================================
Files (%config with parameters)
===============================================================================

%files
%config(noreplace) /etc/myapp.conf
%config(missingok) /etc/optional.conf
%config(noreplace,missingok) /etc/combined.conf

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier
        (config_qualifier
          (config_option)))
      (path))
    (file
      (file_qualifier
        (config_qualifier
          (config_option)))
      (path))
    (file
      (file_qualifier
        (config_qualifier
          (config_option)
          (config_option)))
      (path))))

===============================================================================
Files (shell globbing patterns)
===============================================================================

%files
%{_bindir}/*
/etc/myapp/*.conf
/usr/share/myapp/[abc]*
/var/log/myapp/*.log

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (path
        (macro_expansion
          (identifier))))
    (file
      (path))
    (file
      (path))
    (file
      (path))))

===============================================================================
Files (multiple file qualifiers)
===============================================================================

%files
%ghost %dir /run/samba
%doc %license COPYING

-------------------------------------------------------------------------------

(spec
  (files
    (section_name)
    (file
      (file_qualifier)
      (file_qualifier)
      (path))
    (file
      (file_qualifier)
      (file_qualifier)
      (path))))

===============================================================================
Files (complex %attr expressions)
:skip
===============================================================================

%files
%attr(%{file_mode},%{file_user},%{file_group}) %{_bindir}/myapp

-------------------------------------------------------------------------------
