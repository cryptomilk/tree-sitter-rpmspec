===============================================================================
ELF dependency with empty symbol version (Requires)
===============================================================================

Requires: libc.so.6()(64bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (rich_dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version)
            arch: (elf_arch)))))))

===============================================================================
ELF dependency with symbol version (Requires)
===============================================================================

Requires: libc.so.6(GLIBC_2.2.5)(64bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (rich_dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version
              version: (identifier))
            arch: (elf_arch)))))))

===============================================================================
ELF dependency with empty symbol version (Provides)
===============================================================================

Provides: libssh.so.4()(64bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version)
            arch: (elf_arch)))))))

===============================================================================
ELF dependency with symbol version (Provides)
===============================================================================

Provides: libssh.so.4(LIBSSH_4_5_0)(64bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version
              version: (identifier))
            arch: (elf_arch)))))))

===============================================================================
ELF dependency 32bit architecture
===============================================================================

Requires: libc.so.6(GLIBC_2.0)(32bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (rich_dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version
              version: (identifier))
            arch: (elf_arch)))))))

===============================================================================
ELF dependency with complex symbol version
===============================================================================

Requires: libgssapi_krb5.so.2(gssapi_krb5_2_MIT)(64bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (rich_dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version
              version: (identifier))
            arch: (elf_arch)))))))

===============================================================================
Multiple ELF dependencies
===============================================================================

Requires: libc.so.6()(64bit) libcrypto.so.3()(64bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (rich_dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version)
            arch: (elf_arch)))
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version)
            arch: (elf_arch)))))))

===============================================================================
ELF dependency without version suffix in soname
===============================================================================

Requires: libfoo.so()(64bit)

-------------------------------------------------------------------------------

(spec
  (preamble
    (tags
      (dependency_tag)
      value: (rich_dependency_list
        (dependency
          (elf_dependency
            soname: (soname)
            symbol_version: (elf_symbol_version)
            arch: (elf_arch)))))))
