===============================================================================
Build Section (Empty)
===============================================================================

%build

-------------------------------------------------------------------------------

(spec
  (build_scriptlet
    (section_name)))

===============================================================================
Build Section (append option)
===============================================================================

%build -a
echo "appending to build"

-------------------------------------------------------------------------------

(spec
  (build_scriptlet
    (section_name)
    (scriptlet_augment_option)
    (script_block
      (script_line
        (script_content)))))

===============================================================================
Build Section (prepend option)
===============================================================================

%build -p
echo "prepending to build"

-------------------------------------------------------------------------------

(spec
  (build_scriptlet
    (section_name)
    (scriptlet_augment_option)
    (script_block
      (script_line
        (script_content)))))

===============================================================================
Build Section (%configure macro not confused with %conf section)
===============================================================================

%build
%configure

-------------------------------------------------------------------------------

(spec
  (build_scriptlet
    (section_name)
    (script_block
      (script_line
        (macro_simple_expansion
          (simple_macro))))))

===============================================================================
Build Section (heredoc with preamble-like content)
===============================================================================

%build
cat > file.pc << EOF
key=value

Name: mypackage
Description: A package
EOF
echo done

-------------------------------------------------------------------------------

(spec
  (build_scriptlet
    (section_name)
    (script_block
      (script_line
        (script_content))
      (script_line
        (script_content))
      (script_line
        (script_content))
      (script_line
        (script_content))
      (script_line
        (script_content))
      (script_line
        (script_content)))))
